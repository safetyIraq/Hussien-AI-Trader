buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // تحديث للإصدار 8.4.0 ليتوافق 100% مع سيرفرات GitHub الجديدة ✅
        classpath "com.android.tools.build:gradle:8.4.0"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
