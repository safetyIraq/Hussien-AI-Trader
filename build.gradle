buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // محرك بناء الأندرويد الإصدار المستقر لعام 2026
        classpath "com.android.tools.build:gradle:8.2.0"
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
